# Local state — use remote backend for team use
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# tfvars files contain passwords — only the .example is committed
terraform.tfvars
*.auto.tfvars

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
