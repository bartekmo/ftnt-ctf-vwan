---
title: "Edit SD-WAN template for branch devices"
category: FortiManager
points: 100
scored: true
hints:
  - text: "SD-WAN overlay tempalates are not shown in FortiManager by default. Enable them in **Feature Visibility** tab in Device Manager > Provisioning Templates."
    cost: 20
  - text: "To add overlay objects to SD-WAN templates you have to manually create them. You can do it without interrupting the overlay SD-WAN template wizard by clicking the small **+** sign in the top-right corner of the template selection drop-down."
    cost: 10
refs:
 - url: "https://docs.fortinet.com/document/fortigate-public-cloud/7.4.0/azure-vwan-sd-wan-ngfw-deployment-guide/94813/editing-the-sd-wan-template-for-branch-devices"
   label: "FortiGate documentation: Editing the SD-WAN template for Azure vWAN deployments"
---

## Overview

As the SD-WAN template focuses on a use case where branch devices build multiple tunnels to a single hub rather than single tunnels to multiple hubs you need to modify slightly the SD-WAN template for branch devices.

### SD-WAN branch template

1. Open the branch SD-WAN template you created earlier and linked to SD-WAN Overlay template.
1. move tunnels to both HUBs to the same SD-WAN zone
1. remove WAN1 and port1 from the template

### Configure SD-WAN rule

1. create a new SD-WAN rule for traffic from your branch network to your Azure spoke network (create new address objects)
1. Select the ***Lowest cost*** strategy and add both tunnels to the rule
1. Add the zone and optionally healthchecks