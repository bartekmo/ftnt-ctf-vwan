---
title: "Allow cross-regional traffic"
category: FortiGate
points: 100
scored: true
prober: spoke_ew
hints:
  - text: "Traffic from the spoke to the branch must pass through the FortiGate NVAs, not the native Azure router. This requires a routing intent policy on the hub — look for 'Routing intent' under the hub's settings."
    cost: 20
---

## Allow cross-regional traffic

Every regional spoke server tries to communicate with all other regions. Because of the intent route this traffic is passing your FortiGates and the FortiGates in the other region. Add a firewall policy allowing this traffic (remember about both inbound and outbound connections).

#### Verify

Open your spoke web server to see when other regions "go green" (remember, for this to work it's not enough that you allow traffic, other hubs also need to allow it!).

<EnvVar label="Spoke server public shortcut" field="spoke_server_public" prefix="http://" link />
